# Runs Directory

Training outputs and model checkpoints (not tracked by git).

## Contents

Each experiment creates a subdirectory with:

- `best_model.pth` - Best model checkpoint
- `history.csv` - Training metrics over epochs
- `config.json` - Experiment configuration

Example: `runs/frac50_partial_ce/`
